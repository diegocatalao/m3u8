name: CI

on: [push, pull_request]

jobs:
  lint:
    name: linter using clang-format
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: install lint dependencies
        run: sudo apt update && sudo apt install -y gcc g++ make cmake clang-format
